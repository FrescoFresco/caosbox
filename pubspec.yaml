name: caosbox
description: CaosBox • beta
publish_to: "none"

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase (versiones compatibles entre sí)
  firebase_core: ^3.7.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.0

  # UI de login lista para usar con Google
  firebase_ui_auth: ^2.0.0
  firebase_ui_oauth_google: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
