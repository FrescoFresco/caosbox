name: caosbox
description: Organiza ideas y acciones
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12

  # UI Auth
  firebase_ui_auth: ^3.0.0     # ajustado a la versión válida en pub.dev
  firebase_ui_oauth_google: ^2.0.0  # la última para Google OAuth


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
