name: caosbox
description: CaosBox – Flutter web app with Firebase
publish_to: 'none'

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  firebase_ui_auth: ^3.0.0
  firebase_ui_oauth_google: ^2.0.0
  cloud_firestore: ^5.6.12

  # UI
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
