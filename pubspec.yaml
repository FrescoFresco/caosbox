name: caosbox
description: CaosBox • beta
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase core + auth + UI Google
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  firebase_ui_auth: ^1.12.1
  firebase_ui_oauth_google: ^1.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
