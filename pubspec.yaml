name: caosbox
description: Simple idea/action board – Flutter Web

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  firebase_core: ^2.25.4
  firebase_auth: ^4.17.4
  firebase_ui_auth: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
