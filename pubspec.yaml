# pubspec.yaml

name: caosbox
description: Organiza ideas y acciones
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase core bumped to v4 for Firebase UI compatibility
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^5.6.12

  # Firebase UI
  firebase_ui_auth: ^3.0.0
  firebase_ui_oauth_google: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
