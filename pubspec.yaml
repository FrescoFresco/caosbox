name: caosbox
description: CaosBox – B1/B2 y Enlaces (Flutter Web + Firestore)
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  provider:
  firebase_core:
  firebase_auth:
  cloud_firestore:
  firebase_ui_auth:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
