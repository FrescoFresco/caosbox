name: caosbox
description: Simple CaosBox POC
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }

  # Firebase (últimas versiones estables compatibles)
  firebase_core: ^4.3.4
  firebase_auth: ^6.1.4
  firebase_ui_auth: ^3.6.3
  cloud_firestore: ^6.0.1

  # Utilidades
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

