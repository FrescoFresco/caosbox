name: caosbox
description: CaosBox • beta
publish_to: "none"

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Estado
  provider: ^6.1.2
  collection: ^1.18.0

  # Firebase base
  firebase_core: ^3.8.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.5.0

  # UI de autenticación + Google
  firebase_ui_auth: ^2.0.0
  firebase_ui_oauth_google: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
